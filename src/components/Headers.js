import React from 'react';
import { HeaderContainer, HeaderTitle } from '../styles/StyledComponents';

function Header() {
  return (
    <HeaderContainer>
      <HeaderTitle>Movie Browser</HeaderTitle>
    </HeaderContainer>
  );
}

export default Header;